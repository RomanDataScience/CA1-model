# Legacy Study Snapshot

This folder contains leftover artifacts from an earlier `vip_optuna_theta_gate_v3` run that was created before the study-output path was moved to the repo root.

## What Is Here

- partial trial output files
- scheduler side products such as `.out` and `.sgl`
- at least one saved `*_ms_on_data.json` result
- a combined VIP trace figure

## Why It Exists

The active study directory is now:

```text
batch_runs/vip_optuna_theta_gate_v3/
```

This `src/batch_runs/...` copy is useful only for forensic comparison with the earlier path layout.
