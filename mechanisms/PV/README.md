# `mechanisms/PV/`

This folder contains PV-related mechanisms retained from the broader model-development workflow.

Current contents:

- `nas.mod`
- `kv1_PV.mod`
- `kv3.mod`

The active CA1 microcircuit in `src/netParams.py` does not currently instantiate a PV population, so these files are presently archival or preparatory rather than part of the default run path.
